Operational script to visualize the data:

./launch.bash

It will launch the python generate_stats.py that generates the new stats and plots, and then launch app_graph.py which opens the browser to visualize the results.

graph.ipynb is the Jupyter notebook to debug and play with the code.